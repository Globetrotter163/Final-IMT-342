# Project Baseline v0.1

This document captures the minimal baseline for Sprint 0 (v0.1).

- ROS2 workspace skeleton with minimal packages.
- Preliminary message/service/action interfaces in `warehouse_interfaces`.
- Build verified with `colcon build --symlink-install` (see AGENT_STATE.md).
